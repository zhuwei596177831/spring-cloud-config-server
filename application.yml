spring:
  profiles:
    active: dev
---
spring:
  profiles: dev
  application:
    name: spring-cloud-config-server-dev
---
spring:
  profiles: test
  application:
    name: spring-cloud-config-server-test